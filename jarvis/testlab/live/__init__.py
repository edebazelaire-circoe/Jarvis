"""The `live` execution profile: a REAL provider session behind capability, budget and opt-in gates.

Binding contract: `docs/testlab.md` ("Live profile", "Cost and budget"). Importing
this package costs nothing and calls nothing; see `jarvis.testlab.live.session`
for the four gates a live run must pass before a socket is opened.
"""
