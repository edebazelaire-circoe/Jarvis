# Execution Log

Reserved for implementation agents and the Project Manager to record durable execution notes, decisions made from live repository evidence, validation outcomes, and handoff changes. No implementation progress is pre-populated here.

## 2026-09-17 — Slice 00 (agent 0)

- Handoff mirrored from Drive (51 files). Branch `task/jarvis-category2-test-lab` created from `origin/main@f7e33ad`.
- Blind audit and baseline done. `origin/main` doesn't import because of conflict markers in `realtime_audio.py`. With that resolved in a scratch worktree, the baseline has 9 known unit failures, all unrelated to this task.
- Declared `HUMAN_DECISION_REQUIRED`: D1 (fix `main`), D2 (Task Type waiver). See `slices/00-project-manager/READINESS.md`.
- Human decisions: D1 fix `main` → `b86f228` pushed, branch replayed onto it; D2 Task Type gate waived. Slice 00 → `READY`.

## 2026-09-17 — Slice 01 (implementer)

- Package `jarvis/testlab/` (pure, no I/O/clock/provider): `validation`, `identity`, `profiles`, `diagnostics`, `scenarios`, `runs`. Contract `docs/testlab.md`, linked from `docs/ARCHITECTURE.md` ("Category 2 Test Lab").
- Decisions from repo evidence: integer diagnostic version (every Jarvis schema version is an int); self-describing documents `schema` + `schema_version` (`jarvis.voice_replay` / benchmark suite idiom); fingerprints are bare 64-hex sha256 of canonical JSON (same encoding as `prompt_registry.fingerprint`); times reuse the Conversation Events UTC-ms wire form; `TestRun.join_ids` keys are Conversation Events `TRACE_JOIN_FIELDS`; errors carry a stable `code` like `ReplayFixtureError` / `BenchmarkSuiteError`.
- Ids `tlr-|tls-|tlb-YYYYMMDDTHHMMSSmmmZ-<16 hex>`: time and nonce are arguments (filesystem-safe, chronological for the Slice 02 per-run directory store).
- Profile names imply minimal requirements (virtual: nothing, cost 0; live: a provider; hardware:*: an audio device; only hardware:guided may and must require human_presence). Audio forbids providers. Slice 08/09 may need to revisit `audio`.
- Status machine queued → running → {passed, failed, errored, cancelled, timed_out}, plus queued → {cancelled, errored}. passed/failed only via `complete_run` from the blocking-assertion verdict; inconclusive → errored `assertions_inconclusive`. A DiagnosticSpec needs ≥ 1 blocking assertion.
- Tests are flat `tests/unit/test_testlab_*.py` (orchestrator instruction) instead of READINESS's `tests/unit/testlab/`; shared builders in `tests/fakes/testlab.py`.

## 2026-09-17 — Slice 01 rework (QA findings F1–F9, F11)

- F1: `check_run_against_spec` re-derives every recorded assertion result from `run.metrics` with `evaluate_assertion` (outcome and observed must match, bool never stands in for a number); forged passed runs are rejected.
- F2: name guard redesigned as a final-word rule over closed word sets (secret words anywhere; private content and code words only as final word or before a carrier word; `<qualifier>_code` is a classification code). Accept/reject tables in `docs/testlab.md` and tests.
- F3: value code scan narrowed to unambiguous forms (dunder call/attribute, `eval(` without space, calls on `os.`/`subprocess.`/…, exact imports of dangerous modules, `#!/`). Documented as a heuristic, not the security boundary.
- F4: `TestRun` v1 gains `diagnostic_fingerprint` (verified by `check_run_against_spec`) and `environment` (pattern-checked dotted names, ≤ 32 non-null scalars; open names because later runners add probes; privacy held by the name rules).
- F5–F9, F11: `1e400`/huge ints → `testlab_json_invalid`; thresholds must fit the unit and `eq`/`ne` only on count/chars/boolean; enum choices guarded; `transition_run(RUNNING, failure=...)` raises; `Scenario.from_dict` requires `primitives` (explicit `SHAPE_ONLY` sentinel); single `identity.id_timestamp`.
- 2026-09-17 Slice 01 rework 2: private names deny-by-default (content word anywhere unless final word is metadata; chain_of_thought/system_prompt compounds anywhere), secret `key` rule with closed UI/data qualifiers plus bearer/jwt/passphrase/auth_header/encrypted_content, enum choices value-checked only, `DiagnosticSpec.fingerprint()` semantic (title/descriptions excluded), docs overclaims fixed; conversation_events forbidden names regression-tested (input/bytes/arguments/args allowed on purpose).
- 2026-09-17 PM: Slice 01 approved after 2 QA rounds (F1 verdict re-derivation, redaction name rule made deny-by-default, value code scan narrowed to unambiguous code, TestRun gains diagnostic_fingerprint + environment). 539 narrow tests pass. Carried to later Slices: F10 (`dataclasses.replace` bypasses transitions) goes to the Slice 02 store; the ban on user-identifying `environment` facts goes to the Slice 02/05 capturers.

## 2026-09-17 — Slice 02 (implementer)

- Port `jarvis/testlab/store.py` (pure, sync Protocol `TestRunStore` + `RunQuery`/`RunPage`/`StorageUsage`, `ArtifactWriteLimits`, store errors, `check_run_update`). Test Lab-local instead of `jarvis/ports/v2.py`: only Test Lab callers, and `ports/v2.py` is a parallel-work conflict zone. Sync: local files under an OS lock; async callers use `asyncio.to_thread`.
- Adapter `jarvis/testlab/filesystem_store.py::FilesystemTestRunStore(root)`: `<root>/runs/<run_id>/record.json` (canonical JSON) + artifacts at their `ArtifactRef` path, `<root>/locks/<run_id>.lock`. Temp + fsync + `replace_with_retry` for records and artifacts; creation via hidden staging dir rename; deletion via hidden rename then rmtree; `remove_stale_temporaries` for crash leftovers (unscheduled). Root wiring to `<runtime>/testlab/` left to the composition layer.
- Concurrency: per-run OS lock (`msvcrt.locking` / `flock`, auto-released on crash, per handle) around read-compare-write, plus CAS `update_run(updated, expected=)` failing on status mismatch then on any record difference. F10 closed: `check_run_update` rejects terminal mutation, illegal transitions, identity-field changes, `started_at` rewrite and artifact-ref removal; new refs are re-hashed against stored bytes.
- Corrupt/missing records → `RunRecordCorruptError`; listing/usage report `corrupt` entries (plus stray names and link dirs), never counted or auto-deleted. Artifact paths: `ArtifactRef` rule (extracted as `runs.check_artifact_path`, no behaviour change) + reserved `record.json`, Windows device names, no symlink/junction component, resolved inside run dir, case-collision refusal; per-kind caps checked per chunk; `audio_clip` refused unless `allow_audio`.
- Retention `jarvis/testlab/retention.py`: disabled by default; 30 d, 1000 runs, 2 GiB, `audio_clip` 256 MiB, 128 deletions/apply; pure planner (terminal runs only, oldest first) + apply via `delete_run` (re-checks terminal under lock).
- Capture `jarvis/testlab/capture.py`: git revision read from `.git` files (detached, symrefs, loose, packed-refs, linked worktrees; reftable refused); `dirty` is an injected async probe, default `git status --porcelain -- . :(exclude)data/state` through `jarvis.runtime.worktrees.git` (asyncio). Environment closed set `os, os_release, os_version, machine, python_implementation, python_version, host.cpu_count, host.memory_gib_bucket`, values containing user/host/home fragments dropped. Config snapshot redacts private-name keys (presence kept), home → `~`, fingerprint over the redacted document.
- Purity test extended to `store` and `retention`. Docs: `docs/testlab.md` Storage section; `docs/ARCHITECTURE.md` paragraph.
- Validation: `tests/unit/test_testlab_store.py` (68), `test_testlab_store_retention.py` (16), `test_testlab_store_capture.py` (25); narrow set `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py tests/integration/test_v2_async_conversation.py` 650 passed; repo-wide static scans (`test_v2_architecture`, `test_owner_voice`, `test_surface_reflex_policy`) 124 passed; no `subprocess.run(`/`os.system(`/`shell=True` literal under `jarvis/testlab`.
- 2026-09-17 Slice 02 rework (QA S1-S3, 4-6): write-time artifact manifest `.artifacts.json` makes kind/media type authoritative + commit-time limits (audio opt-in, per-kind cap); Windows path budget (259/247, injectable) -> `testlab_store_path_unsafe`; snapshot URL userinfo/query stripped + credentials.py regression test; dirty probe `--no-optional-locks`; store enforces `ENVIRONMENT_FACT_NAMES`; single-read verify, retention ages from `finished_at`, sweep removes empty subdirs, thread-safe sink counter. 134 store tests, narrow set 675 passed.
- 2026-09-17 Slice 02 rework 2 (QA R1-R2, PM 3-7): deletion hidden as `.deleting-<16 hex>` (shorter than a run id), pending deletions listed as `testlab_store_deletion_pending` and retried by every sweep (long legacy names shortened first); sweep and usage never follow links; snapshot replaces emails with `<email>`, drops SSH remote users, keeps prose punctuation after URLs, URL-path secrets documented as name-only; update checks only added environment names; corrupt manifest listed in `corrupt`; docs temp lengths 28/30 and accepted sweep-vs-put race. 155 store tests, narrow set 696 passed.
- 2026-09-17 PM: Slice 02 approved after 3 QA rounds, with real-process crash, CAS race, junction and path-budget probes on this host. Carried to Slice 05: schedule `remove_stale_temporaries` and retention outside active runs (a sweep can race a put into a reused old empty dir; retryable, documented); call `check_run_against_spec` before a completed run is stored; add new environment probes to both `ENVIRONMENT_FACTS` and `store.ENVIRONMENT_FACT_NAMES`. Carried to Slice 08: default artifact path budget is 171 chars (LongPathsEnabled=0).

## 2026-09-17 — Slice 03 (implementer)

- DiagnosticBundle `jarvis.testlab.bundle` v1 (pure): strict declarative codec, sections turns / speech (delivery outcome + terminals + played_ms) / playback / barge_in / provider / latency (stage joins + producer `reported`) / aggregates / anomalies / references; `coverage` per source (available, empty, truncated, missing, unavailable, not_requested + reason code); every item has evidence refs (`cev-…`, `trace:<offset>`, `report:<sha16>`) that must resolve. `bundle_id = format_bundle_id(session start, content_fingerprint[:16])`; `capture` (captured_at, capture-time code/config) is outside the fingerprint, so re-import is idempotent and the store answers `duplicate`.
- Rules `bundle_rules.py` (table + evaluators, thresholds as data, `evaluated: false` without their source): `voice.self_barge_in` (15 s window), `speech.delivered_after_supersession`, `speech.duplicate_payload`, `speech.missing_delivery_outcome`, `latency.above_threshold`, `events.reconstruction_anomaly` (reuses `reconstruct_conversation` anomalies). Self-barge-in window raised 8 s → 15 s after the real trace showed user turns submitted 9.4 s after a real interruption (long utterance).
- Capture `bundle_capture.py` (I/O): trace reader bisects on timestamps to the window, bounded bytes/lines/line size/selection, region-based counts and `window_end` normalization so a growing journal does not change the bundle; lines projected with the drill-down `project_trace_entry` plus a closed extra scalar key set. Events via the store port, a read-only state DB (`ReadOnlyStateDatabase` = `run_serialized` seam over `mode=ro` + `query_only`, reusing `SQLiteConversationEventStore` decoding) or an export file. Reuses `VoiceSessionMetricRecorder` report files and `trace_summary.summarize` rather than re-deriving them.
- Storage: `BundleStore` port in `store.py`, `FilesystemBundleStore` `<root>/bundles/<id>/bundle.json`; lock/atomic write/path budget/link helpers extracted from `filesystem_store.py` into private `_fs.py` (no behaviour change, 155 store tests unchanged). `redact_urls`/`redact_identifying_text` moved to pure `redaction.py` (re-exported by `capture.py`). No product module outside `jarvis/testlab` changed.
- Live facts: `data/state/jarvis.sqlite3` is still state schema v1 (no `conversation_events` table) and `runtime/trace.jsonl` (ends 2026-09-16 09:06) has no `conversation_event_id`, so the real bundle is journal-only with events `unavailable` / `conversation_events_table_absent`.
- Validation: `tests/unit/test_testlab_bundle.py` (30), `test_testlab_bundle_capture.py` (18), purity extended; `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py` 732 passed; `test_v2_async_conversation.py test_conversation_event_rollout_gate.py` 17 passed; static scans + drill-down tests 187 passed. Opt-in smoke `JARVIS_TESTLAB_REAL_SESSION=1 tests/integration/test_testlab_bundle_real_session.py` passed on the latest real conversation (counts in the Slice report).
- 2026-09-17 Slice 03 rework (QA R1-R5, 6-9): voice session segments bound every rule horizon (coverage.segments + warnings multi_session/long_selection/open_window/torn_tail); codec cross-checks rules/findings, speech outcome vs terminals, refs vs source status; journal damage counted over the physical scan (no_decodable_lines/no_timed_lines, BOM, CR, torn_tail, window_open); latency queue-free stage + turn first audio measured once; self barge-in uses VAD onset (window 20 s); new sections user_activity/lifecycle/brain/usage, rule speech.spoken_diverged, selection budget = mapped kinds + summary kinds, unmapped-kind table; run-store sweep keeps bundle locks; one SafeDiagnostics helper. Real smoke findings: latency 23→20, missing outcome 2→2 (silent_ms 152 000 000→7 050/1 852 with cause), self barge-in 1→1 (a different, played_ms 0 episode), spoken_diverged 0→14 (new rule); 16 segments.
- 2026-09-17 Slice 03 rework 2 (QA Q1-Q5, rule plausibility): self barge-in needs audible output and reads user activity with basis (open onset, onset, commit/transcript/turn submission of an utterance under way); `speech.spoken_diverged` removed from rules (PM decision, counts kept; Issue `speech-payload-integrity-needs-normalized-measure.md`); queue-free wait also waits for the user's floor; idle gap no longer splits an identified session (look-ahead for unidentified lines, data-less session end attaches); codec checks segments, warnings, finding subjects, list order and latency arithmetic. Real smoke findings 40 → 20: latency 20 → 18 (17 brain-delayed turn first audio, 1 slow provider first PCM), missing outcome 2 → 2 (audio native failure), self barge-in 1 → 0, spoken_diverged 14 → 0 (rule removed); segments 16 → 15. All rules still v1 (nothing stored yet).
- 2026-09-17 PM: Slice 03 approved after 3 QA rounds. On the real session, findings went from 40 to 20 (18 latency, 2 missing outcomes caused by a device failure), each checked against trace kinds. `speech.spoken_diverged` was removed as a rule (see Issue). The `voice.self_barge_in` blind spot for transcribed echo is documented. Carried to Slice 06: the harness must emit voice.start/stop/connecting + session_id so segmentation works. Carried to Slice 07: most latency findings are brain delay, so decide on a brain-budget metric. Carried to Slice 12: payload integrity is blocked on a normalized producer measure; bundles are journal-only until the live Jarvis runs state schema v2.

## 2026-09-17 — Slice 04 (implementer)

- Catalog: pure `primitives.py` (registered vocabulary), `implementations.py` (in-code name -> factory/reservation registry), `manifests.py` (`jarvis.testlab.manifest` v1 + `jarvis.testlab.catalog_lock` v1 codec, fingerprint, `check_manifest`), `promotion.py` (pure `promote_scenario`); I/O `catalog.py` (`load_catalog`, `Catalog.list_diagnostics/describe/history/resources_and_cost/check_run`) and `replay.py`.
- Reuse (B3): the `jarvis.voice_replay` v1 codec moved to `jarvis/testlab/replay.py`, its action vocabulary and per-action argument schema became the replay half of the primitive registry (same names, same field types, same validation order, same `fixture_*` codes). `tests/replay/voice_replay.py` is now a re-export plus `ReplayClock`/`ReplayDriver` (execution stays test-only until Slice 06). All 9 fixtures convert 1:1 to Test Lab scenarios and back, provenance and origin preserved, fingerprints pinned in the test.
- Decisions: `at_ms` is a common argument of every step (replay's absolute timeline, not a wait-only model); argument types are global by name (replay rule) so the first fault is stable; `parameter.override` is a prelude at at_ms 0; manifests live in the package at `jarvis/testlab/official/<domain>/<name>.v<N>.json` with `catalog.lock.json` (drift, unlocked, orphan and history-gap all fail the catalog test); availability lives in code, not in manifests, so Slice 06/08/09 register a runner without touching a manifest or bumping a version; promotion writes nothing (human publishes text + lock entry).
- Contract change (documented, tested): `Scenario` gains optional `provenance` (`ScenarioProvenance`, the replay provenance productized, inside the scenario fingerprint) so an incident-derived scenario keeps its evidence through promotion and storage. `diagnostics.check_assertion_threshold` exposed for `expect.metric`.
- Seeds: `voice.self_echo`, `speech.payload_integrity` (harness scripted-vs-delivered payloads only, never the `spoken_diverged` producer signal — see the Issue), `speech.stale_supersession` (carries the converted `stale_ack_35_9s` fixture as its scenario), `voice.queue_latency` (thresholds aligned with the `latency.above_threshold` bundle rule). All v1, `virtual` only, implementation names reserved `runner_not_registered`.
- Validation: `tests/unit/test_testlab_*.py tests/unit/test_voice_replay_fixture.py tests/unit/test_documented_routes.py` 958 passed; `test_v2_async_conversation.py test_voice_replay_regressions.py test_voice_replay_safety_regressions.py` 25 passed; static scans (`test_v2_architecture`, `test_owner_voice`, `test_surface_reflex_policy`) 124 passed. No `subprocess.run(`/`os.system(`/`shell=True` under `jarvis/`, no new dependency, no YAML.
- Carried forward: Slice 06 registers the virtual runners (the reservations name it) and implements `PrimitiveHandler` tables (`missing_handlers` checks coverage); Slices 08/09 widen primitive profile support together with real runners, which bumps the seed versions; Slice 07 owns the score computation the seeds declare; Slice 10 exposes `CatalogEntry.to_dict()`; the seed override allowlists are empty until a Slice verifies real setting paths.
- 2026-09-17 Slice 04 rework (QA P1-P2 + nits): catalog loader refuses Windows junctions through `_fs.is_link` (a junction is not a symlink, so it could reach manifests outside the root), with a `_winapi.CreateJunction` test; `promote_scenario` takes the published history as data (`published={version: fingerprint}` from `Catalog.published_fingerprints`), so `promotion` no longer imports the I/O catalog, `jarvis.testlab.catalog` left the purity allowlist and the purity check itself is now tested against I/O imports and `open`; docs state that the scenario `provenance` key is required (null allowed, pre-Slice-04 documents refused) and that a wording-only re-promotion returns `existing=true` (file may be rewritten, lock untouched); step error paths use the indexed `steps[<i>].args.<name>` form on both the decode and primitive routes. 972 unit + 25 integration passed.
- 2026-09-17 PM: Slice 04 approved (QA verdict: approve, with P1/P2 fixed before commit). The replay decoder move was proven equivalent over ~4,100 differential cases: identical error codes and check order. Carried to Slice 06: register the 4 reserved virtual runners and provide PrimitiveHandler tables (`missing_handlers` shows the gap); only `speech.stale_supersession` ships a scenario, so the other three need scripted runners. Carried to Slice 08: `audio.inject` needs a fixture root, and adding a profile to a seed manifest requires a version bump. Packaging note: `jarvis/testlab/official/*.json` is package data with no pyproject entry (same as control_center.html); only matters for a wheel build.

## 2026-09-17 — Slice 05 (implementer)

- Execution: pure `jobs.py` (`WorkerJob` / `WorkerResult` v1, per-run file names, closed failure-code vocabulary), `runners.py` (`DiagnosticRunner` / `RunContext` / `RunOutcome` / `RunCancelled` seam for Slices 06/08/09), I/O `supervisor.py` (`RunSupervisor`, `RunRequest`, `SupervisorPolicy`, `AdoptionReport`), `worker_launcher.py` (`SubprocessWorkerLauncher` + injectable seam), `worker.py` (`python -m jarvis.testlab.worker <job.json>`), `maintenance.py`, and `selftest.py` (the `selftest.worker` TEST FIXTURE diagnostic, never a seed).
- Worker protocol = files in a per-run scratch (`job.json`, `result.json`, `cancel.json`, `heartbeat.json`, `worker.lock`, `worker.log`, `worker-stderr.log`, `runtime/`, `data/`), the `_run_isolated` idiom (B3): a pipe dies with the worker, a result file survives both deaths and is what adoption reads. The command line carries only the job path.
- The worker reports metrics only; the supervisor derives every `AssertionResult` with `evaluate_assertion`, calls `complete_run`, then `check_run_against_spec` (carried item): an undeclared metric is stored `errored` / `result_contradicts_spec`, never `passed`.
- Isolation: `JARVIS_RUNTIME_DIR` / `JARVIS_DATA_ROOT` inside the scratch with a settings COPY (B4.2, the permanent file is only read), `JARVIS_CORE_TOKEN_FILE` dropped, provider keys emptied (not removed, `load_project_environment` uses `setdefault`) unless the profile declares a provider capability, and a worker-side guard that refuses with `isolation_violation` unless `V2Settings.load()` resolves inside the scratch.
- Overrides come from `ScenarioCheck.overrides` (prelude only) and `RunRequest.overrides`, both restricted to the manifest `override_allowlist`; the same name twice with two values is refused, never silently resolved (carried item).
- Reservation from `Catalog.resources_and_cost(profile).requires` held for the whole run (virtual declares nothing, so virtual runs never contend); authorization is `check_profile_permission` against the caller's `ResourceGrant`. A denied run is persisted `errored` / `permission_denied` (no new status invented, `queued -> errored` is legal) and never started.
- Bounds: startup (spawn -> first heartbeat), heartbeat silence, the profile's declared `max_duration_s` measured from the first heartbeat, cancel grace, then the tree kill (`OwnedProcessTree` Job Object on Windows, documented POSIX `killpg` fallback). A run never stays `running`.
- Adoption at `start()`: the worker lock (OS-released on death) is the liveness test — held means another supervisor owns the run (`active_elsewhere`, never killed by pid, which the venv launcher's pid mismatch also rules out); free means the result file finishes the run (`recovered`) or it becomes `worker_lost` / `run_abandoned`. Orphan scratch directories are removed.
- Maintenance schedules `remove_stale_temporaries` + retention only when no run is active, holding the same gate the dispatcher takes to start a run (carried item: keeps the documented sweep-vs-put race out of the run path); named active ids are removed from the retention plan.
- No new environment probe, so `capture.ENVIRONMENT_FACTS` and `store.ENVIRONMENT_FACT_NAMES` stay equal.
- Validation: `tests/unit/test_testlab_supervisor.py` (46), `tests/unit/test_testlab_supervisor_jobs.py` (17), `tests/integration/test_testlab_worker.py` (10 real-process tests, one process each, two in the tree-kill test). Narrow set `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py tests/integration/test_v2_async_conversation.py` passes; no forbidden execution primitive under `jarvis/`; purity extended to `jobs`.
- Carried forward: Slice 06 registers the virtual runners against `runners.DiagnosticRunner` (a seed run is currently persisted `errored` / `runner_unavailable`, not refused); Slice 07 owns the score (the worker may report one, nothing computes it); Slices 08/09 need `ArtifactWriteLimits(allow_audio=True)` and real device-contention detection before their profiles can reserve a device; Slice 10 composes the supervisor (work root, catalog root, settings path, diagnostics sink) — nothing wires it into the application yet.
- 2026-09-17 Slice 05 rework (QA S1-S5, 6-8): runners get `RunArtifacts` (put/read/open/list of their own run) instead of the store, and a terminal record the supervisor did not write is an `run_concluded_out_of_band` incident instead of a silent early return; queue-wait accrues only while genuinely blocked and `maintain()` wakes the dispatcher, so a submit during an upkeep pass starts when the pass ends (1.9 s, was errored after 6.4 s); adoption clamps the terminal time up to `started_at` and reports `failed` separately from `reaped`; the run scratch is built through no link (a planted junction is removed, never followed); captured evidence is scrubbed of credentials with the canonical `SECRET_PREFIXES`; a measurement written just before a liveness fault is kept (a timed-out run keeps its metrics and stays `timed_out`); a corrupt `result.json` is `worker_result_invalid` and exit descriptions are English; one supervisor per work root (`.supervisor.lock`, `testlab_supervisor_work_root_busy`) plus a documented threat model. 63 unit + 10 integration tests.
- 2026-09-17 Slice 05 rework 2 (QA 1-2 + nits): captured evidence also redacts the value FOLLOWING a credential introducer (`Authorization:`, bare `Bearer`/`Basic`, `api_key`/`secret`/`password`/`token`… before `=` or `:`), which is what let a real worker's `Bearer QABEARERTOKEN` reach both stored artifacts; `RunArtifacts` binds its four operations at construction with `__slots__` and keeps no store attribute, so no attribute chain from a `RunContext` reaches `update_run`/`delete_run`; `redact_evidence`/`failure_detail` moved next to `identity_fragments` in `capture.py` (not `redaction.py`, which is a pure contract module and may import neither the identity probes nor the credential vocabulary), with the 8-character minimums documented as deliberate. 45 supervisor + 69 capture unit tests, 10 integration (the `error` fixture now prints a credential line and the test proves both artifacts are clean).
- 2026-09-17 PM: Slice 05 approved after 3 QA rounds, all exercised with real processes (supervisor-only kill, worker kills at each phase, junction, out-of-band conclusion, two supervisors, no strays). Two real defects were found and fixed: a runner could forge its own verdict through the store, and a `Bearer` token survived into stored logs. Carried to Slice 06: implement `runners.DiagnosticRunner`, register the 4 reserved virtual names, honour `RunContext.cancelled`, and work with JARVIS_RUNTIME_DIR/JARVIS_DATA_ROOT pointing at the scratch. Carried to Slice 07: nothing computes `WorkerResult.score` yet, and sweeps need `max_concurrent_runs` above its default of 2. Carried to Slice 08/09: the store is built with allow_audio=False and the job carries no audio opt-in; real device-contention detection (READINESS B9) is still absent. Carried to Slice 10: nothing composes the supervisor yet (work root, catalog root, settings path, sink, MaintenancePolicy). See Issues/voice-harness-wall-clock-flake-under-load.md.

## 2026-09-17 — Slice 06 (implementer)

- Harness productized (READINESS B4.1): `jarvis/testlab/virtual/` = `patching.py` (`PatchStack`, `setattr`/restore without pytest — `monkeypatch` satisfies the same one-method protocol, so `voice_stack(tmp_path, monkeypatch, ...)` is unchanged), `devices.py` (the two buffered streams moved out of `tests/fakes/audio_device.py`), `harness.py` (the whole harness), `journal.py`, `echo_guard.py`, `executor.py`, `runners.py`, `registry.py`. `tests/integration/async_conversation_harness.py` and `tests/fakes/audio_device.py` are re-export shims; their 6 + 10 consumer modules are untouched and pass unchanged.
- Additive harness changes only, all defaulted off: `FakeRealtimeSession.session_id` (defaults to the session name; it is what makes a trace segment), `cancel_refusals` (armed by `provider.cancel_rejected`), `BrainTurnHandle.say(speech_id=, source=)`, `voice_stack(journal=, core_journal=, session_id_factory=, capture_factory=)`, `VoiceStack.shutdown()` (production stop, so `voice.stop` is journaled instead of a cancelled task). `TIMEOUT_S` 10 s → 30 s: it is documented as "never a real wait, only to fail a blocked test", and the 10 s wall clock is the known flake (Issue `voice-harness-wall-clock-flake-under-load.md`) — it reproduced once here in 4 chunk runs and 3/3 after. No proven sequence changes; Test Lab runs never read it.
- Registration without breaking purity: `ImplementationRegistry.registering(entries)` replaces a *reserved* entry of the same name and profile (refuses anything else); `virtual.registry` holds lazy factories, so a supervisor that runs no virtual diagnostic imports no voice stack. The 5 virtual names are now `available`; the 4 others stay reserved. No manifest edited, no version bumped.
- `VirtualExecutor` covers all 25 virtual primitives — `missing_handlers` is `()` (pinned by a test). Decisions from live evidence: `scheduler.enqueue` goes through the brain turn in flight when there is one and otherwise publishes `brain.speech.requested` straight onto Core's bus with the scenario's own `SpeechSource` (a candidate may predate any turn, which is the stale-ack shape); `owner.rejected`/`owner.confirmed` are OBSERVATIONS, because the owner decision is the code under test; `user.turn` verifies a declared `addressing` and never imposes it. A step the stack does not perform fails the run naming step, primitive, expectation and observation.
- `VirtualEchoGuard` (new double, injected through the existing `capture_factory` seam — `realtime_audio.py` and `voice_v2.py` untouched): without a capture, `_barge_in_allowed()` has no witness and confirms EVERY echo candidate, so `voice.self_echo` measured 1 false barge-in and could not have failed differently on a broken stack. The double reports acoustic state only (fed by the production writer's `push_reference`); the decision stays in `realtime_audio.py`. With it: 0 confirmed, 7 `voice.barge_in_ignored` / `speech_started_behind_echo_guard`.
- Journal: both sinks are `TraceRecordingJournal` (in-memory view unchanged + real `RuntimeJournal` `trace.jsonl` + a stamped timeline the runners measure on). A run's trace carries `voice.start`/`voice.connecting`/`voice.stop` and `session_id = <run_id>-s<n>`; committed as a `trace_excerpt` artifact AFTER teardown (Core writes its last lines while stopping). A bundle captured over it segments into 1 session, 0 warnings, 0 findings. `TestRun.bundle_id` is NOT set: a runner may not write the record and `WorkerResult` carries no bundle id — carried to Slice 10/12, which has the stored trace and the session id.
- Four seed runners, each emitting exactly its declared metrics. Real-worker results: self_echo `{0, 2, true, 1000}`; payload_integrity `{2, 2, 0, 0, 0}`; stale_supersession `{1, 0, true, 28000}` (the fixture's own 28 s, in virtual time) on the shipped scenario; queue_latency `{queue_free 0-5 ms, started_to_audio ~14 ms, turn_to_audio ≈ brain delay, delivered 1}`. No metric turned out unmeasurable, so no manifest v2 was needed.
- Failure paths proven per seed (in process, through documented runner seams or a supplied scenario): a real confirmed barge-in → `no_false_barge_in` + `output_completes` failed; a corrupted delivered payload → `payload_is_faithful` failed; a scenario where the intent is never delivered → `latest_intent_wins` failed; a first audio 4.2 s after the start → `started_to_first_audio` failed.
- Determinism: no sleep as synchronization; every wait is a condition bounded by `min(STEP_TIMEOUT_S, RunContext.remaining_s)`; `_settle()` waits for journal quiescence. The only real wait is the scripted brain's thinking time, which is what `user_turn.end_to_first_audio_ms` measures, through `RunContext.sleep`. Cancel and deadline proven with real workers (`cancelled` / `timed_out`, no tree kill in either detail).
- Validation: `tests/unit/test_testlab_virtual.py` (30) inside `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py` → 1042 passed; `tests/integration/test_testlab_virtual_runners.py` (10, in process) + `tests/integration/test_testlab_virtual_runs.py` (6, one worker process each) → 16 passed; the three harness-consumer files → 25 passed (3/3 chunk runs after the one documented flake). Docs: `docs/testlab.md` "Virtual profile" + the stale Slice 06 forward references, `docs/ARCHITECTURE.md`.
- Carried forward: Slice 07 owns the verdict of a failed `expect.*` step (today `errored`, not `failed`) and the generic scenario runner's measurement contract; Slice 08 inherits the acoustic limits listed in "What the virtual profile cannot prove" and `audio.inject`; Slice 10/12 owns bundle capture + `TestRun.bundle_id` over the stored `trace.jsonl`.
- 2026-09-18 Slice 06 rework (QA vacuity findings 1-4 + nits): `voice.queue_latency` omits a latency metric whose join is missing instead of scoring it 0 (QA's no-audio mutation went passed -> inconclusive, `started_to_first_audio`/`turn_to_first_audio` `missing`) and `delivered_count` now counts audible deliveries only; `voice.self_echo` requires the stack to RECORD a barge-in decision for each injected onset (QA's deaf-to-VAD mutation went passed -> errored/runner_failed with an actionable detail); `VirtualEchoGuard` lost its clock and its 2 s window (far end is state), so the reproduced false failure at a 2.2 s host stall is gone and 2.2 s / 4.0 s stalls now measure identically to no stall; `speech.stale_delivered_count` is keyed on candidate identity (a later `intent_epoch` the scheduler has already seen), proven by an A/B on a new `stale-source-stays-eligible` mutation: pre-rework 0 / `no_stale_delivery` passed, reworked 1 / failed with `stale_wait_ms` 35900, the incident's own delay. Nits: `registering()` refuses a repeated name like the constructor; `test_testlab_catalog.py` now asserts both views (declaration reserved, execution available) and the worker docstring points at it; `virtual/devices.py` raises `DeviceContractError` instead of bare `assert`; `harness.py` is English and says five doubles; `commit_trace` streams. `_settle()` quiescence and the self-echo candidate spacing are documented as known heuristics. No manifest changed and no version was bumped: every metric still matches its declared description. 1048 unit, 28 testlab integration, 25 harness-consumer tests pass. The heavy voice-integration group (`test_*_composition`, `test_conversation_event_*`) flakes 1 run in 7-8 under load; an 8-attempt A/B against a pre-Slice-06 tree failed on attempt 7 on BOTH, on the same test, so it is the documented wall-clock family (Issue `voice-harness-wall-clock-flake-under-load.md`) and not this Slice.
- 2026-09-18 PM: Slice 06 approved after 2 QA rounds with mutation testing against real production faults in an isolated tree. 6 of 8 mutations were caught by the right assertion at first pass; the 2 vacuous passes QA found (no audio at all → latency passed; deaf to provider VAD → self_echo passed) were fixed by omitting missing joins and by requiring the stack to answer each injected onset, both re-verified by QA re-running the mutations. The echo-guard double now reads no clock, which removes a false accusation on a loaded host. Carried to Slice 07: give "could not measure" its own shape — a missing join ends `errored`/`assertions_inconclusive` and an unanswered onset ends `errored`/`runner_failed`, and telling them apart needs the failure detail; also nothing computes `WorkerResult.score` and sweeps need `max_concurrent_runs` above 2. Carried to Slice 08: `audio.inject` is the one virtual-absent primitive and needs a fixture root; `allow_audio` is still off; the acoustic claims virtual cannot make are exactly what the audio/hardware profiles must prove. Carried to Slice 12: bundle capture and `TestRun.bundle_id` over the stored trace (the session id derives from the run id).

## 2026-09-18 — Slice 07 (implementer)

- Pure: `scoring.py` (the declared `weighted_mean`: per-component linear worst->best, clamped, weighted, rounded to 4 dp), `outcomes.py` (`RunOutcomeClass` + closed `(status, failure code) -> outcome` table + `classify_run`), `compare.py` (per-metric deltas with direction, assertion changes, score delta, explicit incomparability, `aggregate_runs` median/min/max, own 5-line median — stdlib only), `sweeps.py` (`SweepSpec`/`SweptParameter`/`SweepPoint`/`expand_points`/`check_sweep_spec`/`SweepRecord`). I/O: `sweep_runner.py` (`SweepRunner`), `filesystem_sweep_store.py`. Purity test extended to the four pure modules.
- Score runs in the SUPERVISOR, from the declaration the run was queued against. `WorkerResult.score` and `RunOutcome.score` are REMOVED, not reserved: nothing computed them, a field the supervisor must ignore is dead contract data, and it was a way for a runner to publish a judgement it has no declaration to justify. A run the supervisor stopped keeps its partial metrics but gets no score, and a score is all-or-nothing (one unmeasured component -> null), because a partial mean is not the declared synthesis.
- "Could not measure" gets its own shape without a new `RunStatus` (Slice 01 forbids one): a derived outcome `passed / failed / inconclusive / refused / crashed / cancelled / pending`, from status + failure code, with `FAILURE_OUTCOMES` covering the whole Slice 05 vocabulary (test-enforced) and an unmapped code reading `crashed`. Two new codes, both `inconclusive`: `measurement_unavailable` (the Slice 06 unanswered onset, was `runner_failed`) and `scenario_expectation_unmet`. A runner says "could not measure" with a typed exception (`runners.MeasurementUnavailable`, which `VirtualRunError`/`VirtualStepError` now subclass, and `runners.ScenarioExpectationUnmet`), never a string the worker must recognise; anything else stays `runner_failed` -> `crashed`.
- `expect.*` verdict rule: an evaluated step that disagrees is a PRODUCT verdict and becomes a measured count (`scenario.expectations_failed_count`), which an ad-hoc diagnostic's own blocking assertion turns into `failed` through the ordinary path — no new verdict path, no runner writing a status. A step nothing could be evaluated against (metric never measured, metric/assertion not declared, no conversation) is authoring/structural -> `measurement_unavailable`. A specialized runner, which cannot carry the count, ends `scenario_expectation_unmet` rather than dropping it. `ScenarioRunner` gains its measurement contract: `scenario.steps_performed`, `.checkpoints_reached`, `.expectations_declared`, `.expectations_failed_count`.
- Comparison compatibility rule: same `diagnostic_id`, `diagnostic_version`, `diagnostic_fingerprint` and `profile`. Different parameters/overrides/code/environment are `differences`, NOT incomparabilities — that is exactly what a sweep varies. Direction awareness needs the declaration (without it every move reads `changed`); `candidate_metrics=` makes cross-version `different_unit` reachable; `passed -> missing` is a regression.
- Sweeps: `SweepSpec` (axes as explicit value lists, `from_range` expanded at construction so the stored declaration is what ran), validated against the diagnostic's `ParameterSpec`s and the manifest override allowlist with `check_parameter_value` — a sweep can reach nothing a single `RunRequest` could not. Bounds refused as a whole (4 axes, 64 values, 256 points, 512 runs, 16 repetitions). One `tls-` id on every run, so the Slice 02 `RunQuery(sweep_id=)` filter is the way back. Fan-out windowed by `min(max_in_flight, supervisor.max_concurrent_runs)`: submitting everything at once would make queued runs accrue blocked time and expire as `resource_wait_timeout`. Partial failure recorded per point, whole-sweep cancel, record rewritten as points land (terminal record immutable), plus a `summary.json` with per-point aggregates, comparisons to the first measuring point and `best_points` — named, never applied.
- Never writes permanent settings: no field, no write path, every swept value is a run-local parameter or override. Proven against a real settings file in the integration test (bytes unchanged) and in the unit test.
- Brain-budget decision (carried from Slice 03): brain delay does NOT become a blocking assertion of `voice.queue_latency`. That diagnostic is about the scheduler's own delays, and on `virtual` the brain time is a run parameter, so asserting on it would test the parameter, not the product. `user_turn.end_to_first_audio_ms` stays non-blocking and informational. A real brain budget needs its own diagnostic (new manifest, Slice 04 path) and is REPORTED FOR APPROVAL, not shipped. No manifest semantics changed: the only manifest edit is the assertion DESCRIPTION of `voice.queue_latency` v1, which dropped a stale "Slice 07 decides" forward reference; descriptions are excluded from both `DiagnosticSpec.fingerprint()` and `DiagnosticManifest.fingerprint()`, verified identical before/after, and `catalog.lock.json` is untouched.
- Scale: the store's list path decodes one record per run and sweeps make it hot. Measured on this host (warm page cache), filtering by `sweep_id`: 2000 runs 2.9-3.1 s per call, 500 runs 0.32-0.40 s. Added a bounded LRU of decoded TERMINAL records (`listing_cache_size`, default 2048, 0 disables) used ONLY by `list_runs`: 2000 runs -> 66 ms, 500 runs -> 17-19 ms after the first call. Sound because a terminal record is immutable by the store's own update rule and a deleted run leaves the directory scan (`delete_run` evicts anyway); `get_run`/`update_run` never consult it, so the CAS still reads disk. No store redesign.
- Validation: `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py` 1205 passed (new: `test_testlab_scoring.py` 27, `test_testlab_scoring_outcomes.py` 23, `test_testlab_compare.py` 34, `test_testlab_sweep.py` 41, `test_testlab_sweep_runner.py` 17, plus 11 `expect.*` rule tests in `test_testlab_virtual.py`); `tests/integration/test_testlab_sweep_runs.py tests/integration/test_testlab_virtual_runs.py` 10 passed. The sweep integration file uses at most 2 concurrent workers and 2-3 points, and mounts the voice stack in one test only. No new dependency; no `subprocess.run(`/`os.system(`/`shell=True` under `jarvis/testlab`.
- Carried forward: Slice 08/09 register their runners against the same seam and should raise `MeasurementUnavailable` rather than letting an exception read as `crashed`; a device/provider profile makes the sweep's reservation contention real, which `max_in_flight` only bounds, so revisit `SweepPolicy` then. Slice 10 exposes `classify_run`, `compare_runs`, `aggregate_runs`, the `SweepStore` and `SweepRunner` (and composes the sweep store root, which is the run store root today). Slice 11 has `RunOutcomeSummary.to_dict()` and the sweep summary document to render without deriving anything. Slice 12 owns the proposed `brain.*` budget diagnostic if it is approved.
- 2026-09-18 Slice 07 rework (QA F1-F3 + 4 nits): F1 `expect.event` asked for `limit=1000` over the store's `MAX_EVENT_PAGE_LIMIT` of 500, so EVERY such step raised and read `crashed`, met or not (nothing in the suite ran one) — it now pages `MAX_EVENT_PAGE_LIMIT` x `MAX_EVENT_PAGES` (20) and refuses to judge a scan it could not finish or a page with `skipped_rows`, because a lower bound cannot decide `count_min`/`count_max`; 6 integration tests RUN the step met and unmet, for the generic and a specialized runner, plus 5 unit tests for the paging edges. Grepping the other two handlers found the same class in `expect.metric`: an unknown comparator or a threshold that does not fit the metric raised `ValueError`/`TestLabError` -> `crashed`; now `testlab_virtual_expectation_unevaluable` -> inconclusive. F2 the listing cache trusted immutability, so a terminal record that BECAME corrupt stayed listed with `corrupt` empty and `list_runs` disagreed with `get_run` in one process; entries now carry `(st_mtime_ns, st_size)` and every listed run is re-stated (8 store tests, both QA repros). Re-measured by `sweep_id`: 2000 runs 1.9-2.9 s uncached vs 140-193 ms warm, 500 runs 0.35-0.39 s vs 37-53 ms (the stat pass costs the 66 ms/17 ms an unvalidated cache reached). F3 the expectation verdict is now a property of the DECLARATION, not of the runner: `expectation_metrics` is shared by every runner, so any diagnostic declaring `scenario.expectations_failed_count` fails on an unmet expectation and one that does not is inconclusive by design. `speech.stale_supersession` v2 (the two expectation metrics + one blocking assertion, everything else identical) is PREPARED AND VALIDATED but NOT published: `slices/07-metrics-scoring-sweeps/proposed-speech.stale_supersession.v2.json`, fingerprint `9cbb198f…`, awaiting approval; v1 and `catalog.lock.json` untouched. Nits: `put_sweep_summary` is write-once like `put_sweep`; `compare_runs` flags a non-terminal run (`run_not_terminal`); `missing <-> failed` documented as `changed`, plus the cache-ceiling note; `assertions_inconclusive` now names the blocking assertions with no measurement. Both Slice 06 vacuity mutations re-run after F1: deaf-to-VAD -> `measurement_unavailable`/inconclusive, no-audio -> `assertions_inconclusive`/inconclusive with the detail naming `started_to_first_audio`. 1227 unit, 18 in-process + 10 worker integration tests pass.
- 2026-09-18 Slice 07: `speech.stale_supersession` v2 PUBLISHED after approval, the documented way — `jarvis/testlab/official/speech/stale_supersession.v2.json` written from `render_manifest`, one lock entry added through `CatalogLock.with_entry(...).render()` (`9cbb198f…`), v1 and its entry untouched (`5be195b3…`), catalog test green (no history gap, no drift, no orphan). The proposal copy in the slice folder was byte-identical and was removed, so there is one shipped record and no duplicate to drift. v2 adds only `scenario.expectations_declared`, `scenario.expectations_failed_count` and the blocking `scenario_expectations_met`; same profiles, parameters and scenario. End-to-end proof through real workers (`test_the_published_v2_turns_an_unmet_expectation_into_a_verdict_and_v1_cannot`): a versionless request resolves to v2 and the shipped scenario plus an unmet `expect.event` ends `failed` with `scenario_expectations_met` failed and the four v1 measurements unchanged, while the same run pinned to v1 ends `errored`/`scenario_expectation_unmet` -> inconclusive. A stored v1 run still passes `Catalog.check_run` and keeps its own `diagnostic_fingerprint`, and `check_run_against_spec` against v2 refuses it; comparing a v1 run with a v2 run reads `different_version` with the two v2-only metrics listed as missing-in-baseline and the four shared ones unchanged. The versionless seed test now asserts v2 and its six metrics; the in-process seed and "cannot carry the count" tests pin v1 explicitly (`build_context(..., version=)`). 1230 unit, 18 in-process + 11 worker integration tests pass.
- 2026-09-18 Slice 07 final pass: the last of the F1 coercion class is gone — `expect.*` arguments are now read through `VirtualExecutor._expect_arg`, which type-checks without coercing (no `int()`, no `str()`, `bool` refused as a count or a name) and turns a missing or wrongly typed argument into `testlab_virtual_expectation_unevaluable` -> inconclusive instead of a `ValueError`/`KeyError` -> `runner_failed` -> crashed; 18 unit tests cover all three handlers. Sweep of the three handlers: no coercion or unguarded subscript left (the only `.args[...]` is inside the guarded helper); the two remaining constructions are `AssertionSpec`/`Comparator`, already inside the try that maps them to unevaluable, and `ExpectationResult`, built from internal values only. The one external-shape assumption left is `item.event.event_type.value` in `_count_events`, deliberately unguarded: a malformed decoded event means the event store itself is broken, which IS a lab defect and correctly reads `crashed`. Docs: three limits of the integrity chain stated and verified on this host — a published manifest's prose can be edited with no drift error (fingerprints exclude it, the same property that keeps a wording-only re-publish comparable), the listing-cache stamp catches mistakes not malice (a same-size `os.utime` edit is served from `list_runs` while `get_run` returns the new bytes — the documented threat model), and `CatalogLock.with_entry` accepts a version hole so only `load_catalog` refuses it (`testlab_catalog_history_gap`), which is why publishing must load the catalog afterwards. Seed table notes that v2's shipped scenario declares no expectation, so its new blocking assertion is vacuous for the seed's own run and only bites for a supplied scenario.
- 2026-09-18 PM: Slice 07 approved after 3 QA rounds. QA found that `expect.event` crashed on every path (limit 1000 vs a 500-event page cap) and that the new listing cache hid a record that became corrupt; both fixed and re-verified, along with the same untested-coercion class in the other expectation handlers. `speech.stale_supersession` v2 was published through the Slice 04 promotion path (PM-approved), which is the first end-to-end exercise of versioning: a v2 run concludes on an unmet expectation while the same run pinned to v1 reads inconclusive, v1 stays valid for stored runs, and every break-it attempt (in-place edit, missing v1, duplicate lock entry, version hole) was refused. Brain delay deliberately stays out of `voice.queue_latency`: on virtual it is a run parameter, so asserting on it would test the parameter. A separate brain-budget diagnostic is proposed and unshipped — Slice 12 owns it. Carried to Slice 08/09: raise `MeasurementUnavailable` for a real acoustic dead end or it reads as `crashed`; sweep reservation contention becomes real with one device (`SweepPolicy.max_in_flight` only bounds submission). Carried to Slice 10: compose the sweep store root and keep ONE store instance (the listing cache is per instance); `RunOutcomeSummary.to_dict()` and the sweep summary are render-ready, so Slice 11 must not re-derive outcomes in JS. Carried to Slice 12: `<root>/sweeps/` has no retention policy.

## 2026-09-18 — Slice 08 (implementer)

- Two profiles, each the virtual stack with one substitution and nothing else. `jarvis/testlab/audio/` (`fixtures.py`, `chain.py`, `runners.py`, `registry.py`) swaps `VirtualEchoGuard` for the PRODUCTION `CaptureProcessor` with the real WebRTC canceller and drives it with an injected fixture through the production capture callback (`capture.process` → `_deliver_capture` → `_put_input`); `jarvis/testlab/live/` (`session.py`, `cost.py`, `runners.py`, `registry.py`) swaps the Realtime double for a real provider session. No device is opened on either, and `audio` needs no provider. Four names registered: `voice.self_echo.audio`, `testlab.scenario.audio`, `voice.queue_latency.live`, `testlab.scenario.live`; only the two `hardware` names stay reserved for Slice 09.
- Three additive seams, all defaulted off, none of them in `jarvis/runtime/`: `voice_stack(audio_class=, realtime_factory=)`, `virtual_voice_stack(capture_factory=, audio_class=, realtime_factory=)`, `FakeRealtimeSession.play_audio(pcm=)` (the canned block is amplitude 1, about -90 dBFS, which a real near-end detector correctly reads as silence — so an acoustic diagnostic must supply its own samples), and `VirtualExecutor.handler_for`, a hook that lets a profile ADD a primitive (`audio.inject`) and never replace one.
- **Device contention (B9)** — `jarvis/testlab/devices.py`, enforced in the SUPERVISOR's reservation path (`_start_ready` → `_external_gate`), before `self._reserved |=` and before a worker exists. The mechanism is read-only evidence the live runtime already publishes: `.voice_heartbeat` freshness against the Control Center's own 5 s window (pinned equal by a test), `.voice_runtime` for who holds it, and a recent non-idle `.voice_state` (a crash mid-sentence leaves `speaking` behind). Fail-closed: one `busy` decides, one `unknown` refuses, a probe that raises becomes `unknown`, and only unanimous `free` frees. Honest limits, all documented: a wedged heartbeat loop or a different runtime root reads free; a Voice process whose device failed to open reads busy; Jarvis starting mid-run is bounded by a 1 s re-probe (`device_contention_during_run` → inconclusive), not prevented. Rejected: probing the Core port (Core runs with voice stopped → false refusals) and an exclusive device open (that IS taking the device, and a WASAPI shared open succeeds even while Jarvis holds it). `DeviceLease` (the `EntryLock` idiom) closes the two-work-roots case; the Control Center's `_audio_test_lock` is an in-process `asyncio.Lock` no other process can see, and teaching the live runtime to take a shared lease is the follow-up Slice 08 may not make.
- **Fixture root** — `jarvis/testlab/fixtures/audio/`, resolved by `AudioFixtureRoot`: the Slice 02 path rule first (a traversal is refused when the scenario is CHECKED, so the worker refuses the run), then a per-component symlink/junction check and a containment re-check on the resolved path. Two shipped clips, both synthetic and regenerated byte for byte by a test (`reference-tone-1s.wav`, `silence-500ms.wav`): no recording of a human voice can live in the root. `resample_pcm16` is hand-written linear interpolation — `audioop` was removed in Python 3.13.
- **Cost** — usage is measured (`voice.realtime.usage`, folded as CUMULATIVE totals: adding them would multiply the bill by the number of updates), price is configuration (no built-in table; `CostModel.from_settings` parses the Control Center `live_pricing` document through `jarvis/runtime/pricing.py`, read from the run's own settings copy, not from a diagnostic parameter), budget is the declared `max_cost_usd`. `CostBudgetExceeded` lives on the runner seam and subclasses `MeasurementUnavailable` with its own code `cost_budget_exceeded` → inconclusive. Stated plainly in the docs: without a price the money bound IS the duration bound.
- Failure vocabulary: `device_contention` and `live_opt_in_missing` → `refused`; `device_contention_during_run` and `cost_budget_exceeded` → `inconclusive`. Every acoustic dead end (no fixture, an unanswered onset, a provider that never spoke, a capture that never became the far end) is `MeasurementUnavailable`, never `crashed`. `WorkerJob.allow_audio` carries the per-run audio opt-in, set only when the caller asked AND the profile can produce audio AND the supervisor's own store allows it; the worker builds `ArtifactWriteLimits(allow_audio=...)` from it and the store still refuses at commit.
- Defect found and fixed while testing: a dispatch pass that raised ended the dispatcher task silently, so every queued run waited forever with nothing in the log. The loop now captures, emits `testlab.supervisor.dispatch_failed` at `error` with the exception type and message, and survives the pass.
- **Manifests PROPOSED, NOT PUBLISHED** (rendered through `render_manifest`, validated by `check_manifest`, and loaded in a temp catalog root with their lock entries): `proposed-voice.self_echo.v2.json` (adds `audio`, fingerprint `cab14029…`) and `proposed-voice.queue_latency.v2.json` (adds `live`, `max_cost_usd` 0.50, fingerprint `6f32cd9e…`), both in `slices/08-audio-live-profiles/`. v1, v2 of `speech.stale_supersession` and `catalog.lock.json` are untouched. `speech.payload_integrity` and `speech.stale_supersession` gain nothing: both measure the scheduler, and neither acoustics nor a provider changes what they can say.
- Validation (foreground, chunked): `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py` 1309 passed (new: `test_testlab_devices.py` 22, `test_testlab_audio.py` 39); `tests/integration/test_testlab_audio_runners.py` 13 + `test_testlab_live_runners.py` 10 passed, 1 opt-in skipped; `test_testlab_virtual_runners.py test_testlab_virtual_runs.py` 25; `test_testlab_worker.py test_testlab_sweep_runs.py` 14; the three harness-consumer files 25. The decisive evidence is a matched pair on the real chain: injected audio that correlates with what Jarvis played gives 0 confirmed barge-ins and a passing run, and a loud uncorrelated near-end voice gives 1 confirmed and a FAILING blocking assertion — the same code, the same schedule, the real canceller deciding.
- What was NOT run, deliberately: no provider call (the live opt-in test is written and skipped; its result is "unverified", not "passed"), and no device-opening test. The contention detector reports this workstation's devices `free`, and the enumeration level of `tests/integration/test_testlab_audio_devices.py` was run under `JARVIS_TESTLAB_AUDIO=1` (3 passed); the `JARVIS_TESTLAB_AUDIO_PLAYBACK=1` level records two seconds of the user's microphone, which is the human's call, not mine.
- Carried forward: Slice 09 inherits the contention gate, the fixture root, the device lease and the `hardware:*` reservations, and owns the FIRST real device open — which must raise `MeasurementUnavailable` (`audio_input_unavailable`, `audio_input_no_signal`, `audio_output_unavailable` from `SoundDeviceAudioDiagnostics`), because the detector answers "is the live Jarvis using the devices", never "can this device be opened". Slice 10 exposes `RunSupervisor.contention`, `RunRequest.allow_audio_artifacts` and the two metadata artifacts. Slice 12 owns the first real provider call, the decision on the two proposed v2 manifests, and teaching the live runtime to respect a shared device lease. `SweepPolicy` still only bounds submission; with a device profile the reservation is now genuinely serialised by the supervisor, so a sweep over a device diagnostic runs one point at a time whatever `max_in_flight` says. `MANIFEST.sha256` at the repository root does not list the two new fixture WAVs (nothing enforces it; their sha256 are pinned by `test_testlab_audio.py` instead).
- 2026-09-18 Slice 08 rework (QA must-fix 1-2 + 5 nits): the fail-closed detector had a fail-OPEN hole — `_read_bounded` collapsed absent / oversized / unreadable into one `None` and `probe` read all three as FREE, so QA's 100 KiB `.voice_heartbeat` and a `.voice_heartbeat` that is a DIRECTORY both reported the microphone free; `_read_signal` now returns `ABSENT` / `UNREADABLE` and only absence is evidence (oversized, locked, a directory, a missing runtime root and an unreadable `.voice_state` all read `unknown`, and a positive BUSY still wins over any of them). The other half of the dispatch defect is closed with one new code `supervisor_fault` -> `crashed`: `_gate_or_fault` turns a raising injected detector into a refusal of THAT run (A/B: unwrapped the run sat `queued` with no `blocked_since` forever, wrapped it ends `errored`/`supervisor_fault` with `testlab.run.gate_failed` at error), and an `_execute` catch-all does the same mid-run with `testlab.run.supervision_failed` instead of dying on stderr and blaming the worker for `worker_result_invalid`; a test asserts no submitted run can stay non-terminal after either fault. Nits: the mid-run probe now runs from SPAWN, so the up-to-60 s startup window is covered (a `silent` worker + a detector flipped to busy ends in under 3 s as `device_contention_during_run` instead of waiting out the startup timeout), and a stop asked for during startup is ended by the cancel grace; `resolve_handler` ENFORCES the add-only rule (`testlab_virtual_profile_handler_invalid`, deliberately not a `MeasurementUnavailable`, so a shadowed primitive reads `crashed`); `echo.coupling_db` is now a declared `ParameterSpec` (float, -60..0, default -12) on the PROPOSED `voice.self_echo.v2` and the runner reads it through the declared path, so it can be swept and is bounded — the proposal was re-rendered, manifest fingerprint `044aea1a…` (was `cab14029…`), still unpublished. Docs: the unreadable/oversized case and the missing root in "Limits, stated plainly", the startup window stated precisely, `supervisor_fault` in both tables, and the fixture sha256 documented as pinned by `test_testlab_audio.py` rather than by the unenforced `MANIFEST.sha256`. 1322 unit, 35 audio/live/worker + 29 virtual/sweep + 25 harness-consumer integration tests pass; the opt-in enumeration level passes with the detector reporting this host free.
- 2026-09-18 PM: Slice 08 approved after 2 QA rounds. QA ran the whole default Test Lab suite (1369 tests) under a plugin patching sockets, every sounddevice entry point and OpenAIRealtimeSession.connect: zero attempts, and every bypass route was refused. The audio profile's evidence is non-vacuous and QA proved the real WebRTC canceller is load-bearing (disable it and the near-end-voice case stops failing). Two defects fixed before commit: an unreadable, oversized, locked or directory heartbeat read `free` in a fail-closed detector, and a raising contention gate left a run queued forever (mid-run it blamed the worker). NOT VERIFIED: no real provider call and no real device open has ever happened — the live opt-in test is written and skipped, and the mic/speaker test was deliberately left for the Human. PROPOSED, AWAITING HUMAN APPROVAL: `voice.self_echo` v2 (adds the `audio` profile + a declared `echo.coupling_db`, manifest 044aea1ad0ba425a) and `voice.queue_latency` v2 (adds the `live` profile, realtime_provider, max_cost_usd 0.50, manifest 6f32cd9e6c85fb8a), both rendered in slices/08-audio-live-profiles/ and unpublished. Carried to Slice 09: the first real device open must map audio_input_unavailable / no_signal / output_unavailable to `MeasurementUnavailable`, since the detector cannot see a third application holding the mic; a transient hardware probe failure must become `unknown` inside the probe, never reach the gate (which correctly reads as a bug). Carried to a future Slice that may touch jarvis/runtime/: the live runtime respects no lock, so detection plus abort is all that protects a live conversation.

## 2026-09-18 — Slice 09 (implementer)

- **Two profiles, `jarvis/testlab/hardware/`**, each the `audio` profile with ONE substitution: `DeviceAudio` IS `jarvis.runtime.realtime_audio.SoundDeviceRealtimeAudio`, so the PortAudio open, the callback, the chunked writer, `push_reference` and the duplex capture are the product's, over this workstation's real speaker and microphone. Four names registered (`testlab.scenario.hardware_auto|_guided`, and the two NEW `voice.self_echo.hardware_auto|_guided`); `testlab.selftest.reserved` is now the only unregistered declared name. No hardware profile calls a provider: the Realtime session is still the deterministic double, so what is real is the microphone, the room, the canceller and the gate.
- **Device selection** — declared `device.input`/`device.output`, else the run's OWN settings copy (`audio_input_device`/`audio_output_device`), else PortAudio's default; normalised by the product's `normalize_device_id`, and the `source` of each choice recorded. Pre-flight through two new 2-line public wrappers on `SoundDeviceAudioDiagnostics` (`check_input_format`/`check_output_format`, delegating to the existing private `_check_input`/`_check_output`) — the only change outside `jarvis/testlab/**`, made rather than duplicating four lines of a canonical implementation.
- **Every device failure is `MeasurementUnavailable` -> `inconclusive`**, with the product's own code kept in the sentence and an uncatalogued code still landing in the family (`testlab_device_<code>`). The three Slice 08 carried forward (`audio_input_unavailable`, `audio_input_no_signal`, `audio_output_unavailable`) plus `device_id_invalid`, `device_open_failed`, `device_output_not_played`, `device_backend_unavailable`. The last two are the **anti-vacuity rule**: "no false barge-in" through a microphone that delivered nothing is an empty run, and an empty run that reads `passed` is worse than an inconclusive one. Release is unconditional (`hardware_voice_stack` closes every `DeviceAudio` in its own `finally`, after the runtime teardown already did), proven on cancel, on a deadline in the past and on a runner that raises.
- **`AudioBackendProbe`** answers "can this host reach PortAudio at all", `free` or `unknown` and never `busy` (a shared-mode open proves nothing), and **never raises** — a transient native failure becomes `unknown` evidence naming its cause INSIDE the probe, because an exception reaching the gate is `supervisor_fault`, which means "our defect", and a missing sound driver is not. Offered as `hardware_contention_detector`, NOT wired into `default_contention_detector`: adding a native query to every device reservation is a cost Slice 10 should choose.
- **Guided contract** — `GuidedPrompt {prompt_id, action, text, deadline_s, phrase, expects_voice, strict_timing}` / `GuidedPrompter.present -> PromptReply`. The presenter reports the ANSWER, never the timing: `GuidedSession` stamps both sides and owns the deadline, so no presenter can understate a delay and every presenter is bounded identically. Four actions (`remain_silent`, `say_phrase`, `interrupt`, `acknowledge`), and the deadline is part of the contract, not a hint — a step with no visible countdown is indistinguishable from a frozen run. `HeadlessPrompter` is the double; `FilePrompter`/`PromptWatcher` is the real channel (`prompt.json` / `prompt-ack.json` beside `cancel.json`, matched by a `sequence` so a stale ack can never answer the next step), and Slices 10/11 only render.
- **Human-as-actor** — timed out / refused / prompter broke -> `MeasurementUnavailable`, never a product failure; late -> evidence, and void only on a step that declares `strict_timing` (the `interrupt` step, where timing IS the measurement). A human who does the WRONG thing is recorded as an observation and the diagnostic decides: speech during `remain_silent` voids a self-echo claim (`testlab_guided_step_not_followed`), the same speech during `interrupt` IS the measurement. "Did they make a sound" is the PRODUCT's near-end signal, not a threshold of ours — on a hardware profile the raw microphone carries Jarvis's echo at exactly a voice's level — combined with one control: the room's level with nothing playing, because a sound that predates the far end is that room's noise floor and the detector is right about that. `guided_prompts.json` (`jarvis.testlab.guided_transcript` v1) is committed in a `finally`, so a run a human abandoned keeps the step that stopped it.
- **Presence is the fifth mechanical gate** — new failure code `human_presence_missing` -> `refused`, checked in `_external_gate` after the provider opt-in and before contention and the lease, so a refusal names the first cause and the one thing that TAKES is still taken last. `JARVIS_TESTLAB_GUIDED=1` is the operator saying "I am here"; the declared `human_presence` capability says the diagnostic needs a person.
- **Vocabulary** — new `human` family, `hardware:guided` only, data-only, strict schemas: `human.silence`, `human.speak`, `human.interrupt`, `human.acknowledge` (+ two arg specs APPENDED, `prompt_id` and `deadline_ms`, whose 10-minute bound is pinned equal to `MAX_PROMPT_DEADLINE_S` by a test). `GuidedExecutor` ADDS them; `resolve_handler` still enforces add-only. `audio.inject` keeps its declaration and gains the one meaning a hardware profile can give it (play through the real output, not push into the capture queue). **One narrowing:** `user.speech`/`user.interrupt` no longer claim `hardware:guided` — that was a declaration written before anything implemented it, and a step that asks a PERSON for something is not measurable without an id, a deadline and an acknowledgement. No published manifest referenced either on that profile; docs and the documented-table test updated.
- **PUBLISHED** (approved in the handoff, through `render_manifest` + `CatalogLock.with_entry`, catalog re-loaded): `voice.self_echo` v2 `044aea1ad0ba425add02930bbd4440bf47d5745b89419473debc9270158372e4` (adds `audio` + the declared `echo.coupling_db`) and `voice.queue_latency` v2 `6f32cd9e6c85fb8a895255c5f4d89d38ee6ce7cf1c6f6f51f2084d256ed23e07` (adds `live`). Lock diff: two entries appended, nothing edited. The audio and live runner tests now READ the published v2 instead of rebuilding it by hand, so runner and declaration cannot drift.
- **PROPOSED, NOT PUBLISHED**: `voice.self_echo` v3, `0ef61abff9fef98b9ada2077f51e0040225d2769d13ea68a564fae88c4ed2430`, in `slices/09-hardware-guided/proposed-voice.self_echo.v3.json` — adds `hardware:auto` (300 s) and `hardware:guided` (900 s, `human_presence`), the parameters `device.input`/`device.output`/`human.interrupt_phrase`, the metrics `barge_in.true_confirmed_count`, `guided.prompt_count`, `guided.late_prompt_count`, and ONE NON-BLOCKING assertion `real_voice_interrupts`. Non-blocking deliberately: assertions are diagnostic-level, so a blocking one on a guided-only metric would make every `virtual` run of v3 inconclusive. That is a limit of the current contract and the Human should know it. `voice.queue_latency` gains nothing from hardware without a mic-side onset detector (a device adds output latency the product does not attribute), and the two `speech.*` seeds measure the scheduler, where acoustics change nothing.
- **Tested with doubles only.** `tests/fakes/sounddevice_double.py` is `sys.modules["sounddevice"]`, so the production open/callback/writer/capture and the REAL WebRTC canceller all run over a room that is arithmetic. The room is driven BY the speaker, block for block: a microphone running faster than the speaker empties the canceller's reference queue while Jarvis is still talking, the far-end window expires and the gate opens — a false positive manufactured by the harness. Two runner rules came out of that and are product-correct, not harness-correct: no candidate before 400 ms of output (the canceller's own pre-roll; earlier measures its convergence), and the output never stops while a decision is pending. The decisive pair passes deterministically: the room's echo gives 0 confirmed barge-ins and a passing run, a loud uncorrelated voice raised at the onset gives at least 1 and a FAILING blocking assertion.
- Validation (foreground, chunked): `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py` 1382 passed (new `test_testlab_hardware.py` 60); `tests/integration/test_testlab_hardware_runners.py` 23 passed, run three times; `test_testlab_hardware_devices.py` 4 collected, **all skipped**; audio 15, live 10 + 1 opt-in skipped, virtual/bundle 25 + 1 skipped, worker/sweep 14; the narrow voice guard 92 passed; `tests/unit/test_audio_devices.py` 6 passed.
- **NOT VERIFIED, deliberately**: no real microphone or speaker has ever been opened by this code, and no human has ever been prompted by it. `tests/integration/test_testlab_hardware_devices.py` is written and left skipped behind `JARVIS_TESTLAB_HARDWARE=1` (+ `JARVIS_TESTLAB_GUIDED=1` for the human half); its result is "unverified", not "passed". The operator script for HV-TL-HW-01 is `slices/09-hardware-guided/operator-script.md`: about five minutes, four steps, with what a good result and a reportable result look like.
- Carried forward: Slice 10 exposes `hardware_contention_detector`, `PromptWatcher` (the CLI presenter reads `pending()` and calls `acknowledge()`), and the `JARVIS_TESTLAB_GUIDED` gate; Slice 11 renders the same prompts with the countdown the contract requires. Slice 12 owns the decision on `voice.self_echo` v3 and the first real provider call. Still open from Slice 08 and unchanged: the live runtime takes no lock, so detection plus abort is all that protects a live conversation. `MANIFEST.sha256` at the repository root does not list the two published manifests or the new modules (nothing enforces it).
- 2026-09-18 Slice 09 rework (QA must-fix 1-3 + the two "also" items): the guided flake was a DEFECT, not a test artifact. Phase 2 judged "the human followed the step" on a detector TRANSITION alone, so a person who began a moment before Jarvis was recorded as not having interrupted; both phases now use the same three sources (the product's `near_end` signal, the room measured with nothing playing BEFORE the step, and the same measurement AFTER it), and the residual blind spot — a sound shorter than the step that the product itself did not call — is documented rather than papered over. Two further defects fell out while proving it: `played_output_ms` is the cursor of the CURRENT output and returns to zero when the device releases it, so reading it after a confirmed barge-in reported "nothing was played" for exactly the case the run wants (sampled during playback now, `RoomPlayback.played_ms`); and the interrupt phase raised the provider VAD onset even with a silent room, where what the gate decides is the canceller re-converging on a second utterance — reported as "a real voice got through". A provider reports speech when there IS speech, so the onset may now DECLINE (`heard_voice_onset`), and a phase that played nothing fails naming the phase instead of blaming the person. Honesty rule: the human acknowledged the interrupt and nothing was confirmed now raises `testlab_guided_claim_unmeasured` → inconclusive, unless the declaration carries a BLOCKING assertion on `barge_in.true_confirmed_count`, in which case the zero is reported and the supervisor fails the run — the declaration-decides rule `expectation_metrics` already uses. `as_device_failure` no longer synthesises a code from a product message (`testlab_device_AUDIO WEIRD/code`): one stable `testlab_device_unmapped`, with the original code in a bounded single-line detail. `check_device_evidence` also requires one sample above the DIGITAL floor (−90.3 dBFS), because a dead input streams as many bytes as a live one. New declared metric `barge_in.first_candidate_offset_ms` makes the 400 ms convergence gap visible, and `declared_metrics` lets the DECLARATION select what a runner offers so an undeclared metric can never fail a run. Tally: **13/13 green** on `tests/unit/test_testlab_hardware.py` + `tests/integration/test_testlab_hardware_{runners,devices}.py` (93 passed, 4 skipped each); 1392 unit; 39 audio/live/worker/sweep + 1 opt-in skipped; 89 narrow voice. PROPOSED, both unpublished, per the PM decision: `voice.self_echo` v3 `3fe5e82a08c7cc671c05311e98a963c0097b81c8a577b528220fc11d9b9f5210` (hardware:auto only, the negative claim) and the new guided-only `voice.barge_in_response` v1 `b2c33aaa528ffdeebb455cc4e51d9bbab1688c5788c166ebb2726f4354c902a8` (blocking `real_voice_interrupts` ge 1).
- 2026-09-18 Slice 09 rework 2 (QA must-fix A-D + the four nits): QA's third defect was real in both directions and lived entirely in the gap between the ~1.2 s every test used and the 8 000 ms the manifests DECLARE. **A**: the interrupt step judged "a person was heard" on the in-window microphone peak (which carries Jarvis's echo at a voice's level) and on the bare near-end signal (which also rises on the canceller's residual), so an EMPTY room certified the one claim only a person can make — deterministically from 2 500 ms up, `passed`, with `real_voice_interrupts ge 1` blocking. Both claims now go through one `RoomVoice` built identically in both phases: `certain` is the two SILENT-WINDOW levels, which nothing Jarvis does can raise; `suspected` is the near-end signal. "Somebody spoke" needs `certain`; "nobody spoke" is voided by `certain`, and by a suspicion only when that phase also confirmed a barge-in (a bare suspicion must not void it, or every long run is inconclusive). The onset now fires only above the echo-only level measured in the SAME run's silent phase plus 6 dB, behind one helper `DeviceAudio.heard_voice_since`, and every record stores the provenance (`room_before_dbfs`, `room_after_dbfs`, `window_peak_dbfs`, `near_end_seen`, `voice_floor_dbfs`, `certain`). **B/C, fork answered — hypothesis 1, with a caveat that matters**: on the `audio` profile the deterministic 8 000 ms failure was the Test Lab's own echo model. An exact, delay-free, noiseless copy is cancellable in a way no room is, the canceller converges until `NearEndDetector` learns a coupling of about −49 dB against its −15 dB warm-up clamp, and every observed cut-off is at 3.2–3.5 s, immediately after the clamp lifts at 3 s. Measured: ideal copy 0/0/**1 FAILED**, delay-only 1, drift-only 1, noise-only 1, all three together **0/0/0 and the full 8 000 ms played, 5/5**. The shipped echo is now a room response (`room_response`: one block of delay, ±3 dB drift, broadband noise) and the audio seed honours the 400 ms pre-roll; the double's `FakeRoom` got the same model plus a −65 dBFS room noise floor, because digital zero is physically impossible and drives the detector's floor to its own minimum. CAVEAT, and it is written up in `Issues/self-barge-in-after-seconds-of-speech.md`: with a realistic room the hardware double still confirms **intermittently** (about 1 run in 4) at 8 000 ms, and no double can say whether that is its remaining artefact or Jarvis's real behaviour — HV-TL-HW-01 at `output.duration_ms = 8000` is the measurement that settles it, and if it confirms with a real room this becomes the defect `voice.self_echo` was written to find. The product was NOT touched. **D**: both acoustic seeds now run at their declared defaults in the default suite (audio ~2 s, each hardware profile ~7 s), and those cases assert the DIAGNOSTIC — it measures, decides on every candidate, honours the pre-roll, reports a verdict derived from its metrics, and never certifies an empty room — never a particular acoustic outcome, because that outcome IS the measurement. Nits: `as_device_failure` no longer synthesises a code; `check_device_evidence` also requires one sample above the digital floor; `voice.barge_in_response` drops `echo.candidate_count` to 3 with both parameters documenting what they cost a human. Tally: 13/13 green on `tests/unit/test_testlab_hardware.py` + `tests/integration/test_testlab_hardware_{runners,devices}.py` (96 passed, 4 skipped each); 1392 unit; 58 audio/live/worker/sweep/virtual + 1 opt-in skipped; 16 audio; 89 narrow voice. Proposals re-rendered, both unpublished: `voice.self_echo` v3 `3fe5e82a08c7cc671c05311e98a963c0097b81c8a577b528220fc11d9b9f5210`, `voice.barge_in_response` v1 `3350bc14fd46d9b9ade40afacfd9880e5605d52cf65aed2267e4352c67b8d1a0`.
- 2026-09-18 Slice 09 rework 3 (PM: the true-positive case is red). Reproduced at once and it was deterministic, not flaky: `test_a_real_voice_in_the_room_is_confirmed_as_a_barge_in` failed **10/10 in isolation** while my suite-level tally was green, so the previous round's 16/16 was measuring the wrong thing. The miss was in the RULE, not in the double: instrumenting `NearEndDetector.update` showed the product reporting `last_near=True` for **all 310 frames** of the voice at mic −6.6 dBFS against floor −70 and coupling −25, and emitting **nothing** — because `NEAR_END` is an EDGE that fires only at the instant speech is CONFIRMED, after which the detector stays `latched` until Jarvis falls silent. The gate had already latched on the echo, so a genuine human voice raised no signal at all. A real person interrupting after the gate had latched once would have been missed identically, and the guided run would have told them it did not hear them. The fake voice was never the problem (≈ −0.0 dBFS against a −65 dBFS room, 65 dB of SNR — a normal speaking voice at a laptop mic), so no fixture level was changed. Fix at the product's own seam: `NearEndWatch`, a `CaptureObserver` passed through `build_capture(observer=)`, counts the PER-FRAME `CaptureFrameContext.near_end` verdict, which is delivered for every frame whatever the latch and the gate are doing. `DeviceAudio.heard_voice_since(mark)` now requires `NEAR_END_FRAMES` = 6 frames = 60 ms (the detector's OWN `min_run_frames`) since the mark **and** the detector currently holding its latch: frames alone fire before the stack would act, the latch alone can be left over from the echo and fires before the voice has arrived. `ECHO_MARGIN_DB` is gone — a decibel margin over the echo asked a person to beat a level the product had already discounted. A second, separate defect surfaced underneath it and was mine from rework 2: the test's `raise_onset` override dropped its `return`, so every onset read as DECLINED and the run raised none at all; it had been passing for the wrong reason (the capture's own edge reaching the bridge) until the room got a realistic noise floor. `raise_provider_onset` now refuses `None` outright — it is our defect, not a decline — with a unit test. Both directions re-measured at 1000/3000/8000 ms: an EMPTY room is never certified (refused at all three, `certain=False`, room −65.2 dBFS) and a real voice is always measured (`certain=True`, room −0.0 to −0.4 dBFS); the margin between the two is **65 dB** against a −50 dBFS threshold that sits 15 dB above the room and 50 dB below a voice — no tuning in it. Note `near_end_frames` is 10/48/60 in the EMPTY room, which is why the claim is gated on the silent-window level and never on frames. Both DEFAULT-duration guided cases now assert the INVARIANT — with a person in the room the claim is measured or honestly refused, and an empty room never reaches a verdict — instead of one refusal code, because which honest refusal you get at 8 000 ms depends on what this room's residual did (nobody spoke, a silent phase that could not be attributed, or the runner's own anti-vacuity rule firing because the stack decided on no onset). Pinning one of them would pin the double's acoustics. Tally after the fix: true positive **15/15** in isolation; the Slice 09 three-file set **13 consecutive runs, 97 passed / 4 skipped**, zero red; 1393 unit; 58 audio/live/virtual/worker/sweep + 1 opt-in skipped; 16 audio; 89 narrow voice.

## 2026-09-18 — Slice 10 (implementer)

- Composition `jarvis/testlab/composition.py` (`TestLabConfig` / `TestLab` / `build_test_lab`): every root from `V2Settings` / `JARVIS_RUNTIME_DIR` under `<runtime>/testlab/` (runs, sweeps, bundles and locks share the root — ids are prefixed — and the SUPERVISOR WORK ROOT is `<root>/work`, never the store root, because `_remove_orphan_scratch` deletes every unclaimed directory under it and would take `runs/` on the first `start()`). Lazy: building a Test Lab reads no file and makes no directory (tested). ONE store instance is shared (the Slice 07 listing cache is per instance). The contention detector reads the LIVE runtime root; `hardware_contention_detector` is composed only behind the hardware opt-in, so no native PortAudio query costs an ordinary reservation.
- **Grant**: from the PROCESS ENVIRONMENT, never a request or a flag — `JARVIS_TESTLAB_LIVE=1` -> realtime/llm provider, `JARVIS_TESTLAB_HARDWARE=1` -> the two device capabilities + the backend probe, `JARVIS_TESTLAB_GUIDED=1` -> human_presence, `JARVIS_TESTLAB_MAX_COST_USD` -> the per-run budget (mistyped reads 0, the safe direction), `JARVIS_TESTLAB_AUDIO_ARTIFACTS=1` -> the store's audio opt-in. Default is empty = exactly `virtual`. `narrow_grant` intersects whatever a caller asks with that one, so a body can only ask for LESS; every other gate stays in the supervisor's reservation path. Two names are new (`JARVIS_TESTLAB_HARDWARE`, `JARVIS_TESTLAB_AUDIO_ARTIFACTS`): the supervisor gates devices by contention + lease, not by a variable, so there was none to reuse.
- Facade `jarvis/testlab/api.py` (`TestLabApi`): list/describe, submit/status/cancel/query/show, artifacts, compare, sweeps (blocking `run_sweep` for the CLI, non-blocking `start_sweep` for HTTP — the id handshake reads `SweepRunner.active_sweep_ids` and then waits for the record to be ON DISK, because an id whose GET 404s is worse than a later 202), bundle capture and listing, guided prompt read/acknowledge, status, retention plan and upkeep. No domain logic: `classify_run`, `compare_runs`, `plan_retention` and the stored sweep summary are returned as they are. Every synchronous store call goes through `asyncio.to_thread`.
- CLI `python -m jarvis.testlab` (`cli.py` + `__main__.py`), 15 commands, human-readable or `--json` (payload alone on stdout; every live line on stderr). Exit codes 0 passed / 1 failed / 2 usage / 3 inconclusive / 4 refused / 5 crashed / 6 cancelled / 7 command failed / 8 no verdict yet, closed over `RunOutcomeClass` and pinned by a test. `run_cli` — not `main()` — is the failure boundary, so tests and the entry point get the same code and the same sentence. Only `run`, `sweep`, `cancel` and `guided` take the work root; a read command keeps working while the Control Center holds it (tested both ways).
- Presenter `jarvis/testlab/presenter.py`: the prompt, the phrase, a bar and spinner that move, the seconds left and `[Enter]/[r]`, with the countdown computed from the WORKER's `shown_at` so it can never promise time the run will not wait. Needed one additive method on the Slice 09 channel, `PromptWatcher.pending_shown()` (`pending()` now delegates to it, same shape, its tests unchanged). When the deadline passes it sends NOTHING, says how long it waited, and refuses to let a keystroke typed afterwards answer the next step.
- HTTP `jarvis/testlab/http.py`, 20 routes under `/api/testlab/...`, each wrapped in one boundary that answers `{ok:false, code, error}` with the real code AND writes a journal line (404/409/503/400/500, unmapped = 500 on purpose). Submitting a run or a sweep answers 202 with the id; `?wait_s=` (≤ 30 s) long-polls and always answers with the current record. The supervisor is started by the FIRST request that needs it, not at boot, so the Control Center never fails to start because a CLI sweep holds the work root — that case is a 409 naming it.
- **`jarvis/runtime/control_center.py`: +17 / -3** (8 of the added lines are comments; two logic lines replaced) — a `TESTLAB_ROUTE` constant + `READ_GUARDED_ROUTES`, the middleware condition turned into an `any(...)` over that tuple (so Test Lab evidence is read-guarded like conversation history: GET included), and a local import + one `install_testlab_routes(...)` call. Nothing else outside `jarvis/testlab/**`.
- Docs: `docs/testlab.md` "Native API, CLI and HTTP" (composition, the grant table, the CLI command table between `<!-- cli-commands -->` markers, exit codes, streams, the presenter, the route table between `<!-- testlab-routes -->` markers, the status table, the JSON shapes, and the limits stated plainly), plus the validation commands; `docs/ARCHITECTURE.md` gained the Slice 10 paragraph and its stale "the hardware profiles come in a later Slice" sentence was corrected. Two tests compare the documentation with the program: the CLI table against `build_parser()`, and the route table against the registered routes, both ways.
- Validation (foreground, chunked): `tests/unit/test_testlab_*.py tests/unit/test_documented_routes.py` 1489+ passed (new: `test_testlab_composition.py` 19, `test_testlab_cli.py` 24, `test_testlab_presenter.py` 21, `test_testlab_http.py` 41); `tests/integration/test_testlab_cli_e2e.py` 21 + `test_testlab_http_routes.py` 16 passed (real workers, one at a time, the `selftest.worker` fixture only); the narrow voice guard 92; the Control Center suite 268; `test_testlab_worker/sweep_runs/virtual_runners/virtual_runs` 39; static scans 124. No `subprocess.run(`/`os.system(`/`shell=True`, no new dependency, no `JARVIS_TESTLAB_*` opt-in set in the default suite, no provider call and no device opened.
- **Not mine, found while validating**: `jarvis/testlab/hardware/runners.py` is MODIFIED IN THE WORKING TREE by another session (`near_alone_frames`, `heard_alone`, `HARDWARE_STEP_TIMEOUT_S`, +181/-62), and three `tests/integration/test_testlab_hardware_runners.py` guided tests fail against it (`observed_voice` is True in an empty room). The test file is unmodified, so those failures come from that in-flight edit, not from Slice 10. I left the file alone.
- Carried forward: Slice 11 renders `outcome` / `RunComparison.to_dict()` / the sweep summary and must derive nothing; it polls `GET /api/testlab/runs/{run_id}?wait_s=` and renders the prompt pair with `remaining_s`. `cancel` only reaches the supervisor of the process that owns the run (`held: false` otherwise) — a browser cannot stop a CLI run and vice versa. The CLI guided path has no stdin seam, so `--guided` end to end (a human typing) is only covered by the presenter's own tests plus the real-channel test over a run scratch. `<root>/sweeps/` and `<root>/bundles/` still have no retention (Slice 12).
- 2026-09-18 Slice 09 rework 4 (PM B1, B2, S1-S4 + nits): the guided diagnostic was not fit to put a person in front of at its declared defaults, and both causes were asymmetries. **B1** — the silent phase raises `echo.candidate_count` candidates and the stack rejects each, which teaches the detector that this near-end is echo (`_release_near_end(learn=...)`); the positive half then had exactly ONE onset to overturn it. Replaying the repo's own defaults scenario 10x gave 4 measured, 3 FAILED with the person at full scale for the whole 8 s and 807-857 near-end frames, 3 refused. The interrupt phase now gets the SAME number of chances, stopping as soon as the gate opens, each still gated on the person being audible so an empty room gets none. **B2** — the positive claim was discarded precisely when the barge-in SUCCEEDED: a confirmation stops the playback, `measure_room` samples ~50 ms later, and a person who said one phrase is silent by then, so the better it worked the emptier its own evidence was (5 runs, ZERO measurements, one with the gate open and the person plainly heard). `NearEndWatch` now also counts near-end frames recorded WHILE THE FAR END IS SILENT, which cannot be Jarvis by construction — as an unbroken RUN, not a total, because an empty room yields 3-6 isolated ones per thousand frames and six of those certified a person who was not there. That still missed a phrase spoken entirely over Jarvis, so a third source was added and it is the cleanest of the three: the silent phase is a within-run CONTROL — same utterance, same volume, same room, no person — so its in-window peak IS this room's echo, and `VOICE_OVER_ECHO_DB` (10 dB) above it is something the stimulus does not explain. Measured separation about 21 dB. **After, over 30 replays of each scenario: B1 27/30 measured and B2 29/30, and every single measured run reported `barge_in.true_confirmed_count >= 1` — zero runs where a compliant human went unheard, against 3 in 10 before.** The rest are honest `inconclusive`s (the stack answered none of the onsets) which now say exactly that instead of reporting a harness timeout. **S2, and this is the diagnostic's whole point** — `require_silent_phase` aborted on `confirmed and near_end_seen`, but a confirmation REQUIRES a near-end candidate, so that fired on every run where the gate opened on Jarvis's own echo and the blocking `no_false_barge_in` assertion was only ever evaluated against zero. It now aborts only when a person CERTAINLY made a sound (the phase-2 standard); otherwise `barge_in.false_confirmed_count` is REPORTED and the declared assertion decides. The finding is visible for the first time: **10 of 20 replays at the default**, which is why the Issue's "about 1 run in 4" is corrected — the defect did not get worse, it became reportable. **S1** — the defaults test early-returned on two codes and accepted any verdict, so all three B1 failures were green; it now repeats and asserts the DISTRIBUTION (at most one refusal, and every measured run heard the person). **S3/nits** — the transcript carries `barge_in_decided`/`barge_in_confirmed`, `window_frames` beside the frame counts, `near_alone_frames`/`alone_run_frames`, `control_peak_dbfs`/`louder_than_control`, and the two levels are no longer merged into one field; every hardware wait goes through `hardware_wait` (45 s budget, hardware failure code) so a hardware run no longer reports `testlab_virtual_run_failed`; requiring every onset to be answered individually is relaxed to the anti-vacuity bar of at least one, since an onset raised after the output ended is never answered. **S4** — docs and the operator script match the code: the script no longer claims the operator must keep speaking, and an echo-triggered confirmation is presented as THE finding rather than "try again somewhere quieter". Counts: Slice 09 three-file set **10 consecutive runs, 97 passed / 4 skipped**; 1498 unit; 58 audio/live/virtual/worker/sweep + 1 opt-in skipped; 89 narrow voice.
- 2026-09-18 Slice 10 rework (QA B1-B3 + S1-S5 + 4 nits): **B1** the presenter answered FOR the human at EOF (`_stdin_line` returned `""` and `read_answer("")` is acknowledged), so `--guided` from a script or CI silently confirmed every step — a distinct `NO_INPUT` now separates "nobody there" from a bare Enter, one of them takes the presenter out of the answering business for the whole run, and `--guided` is REFUSED before anything is queued when stdin is not a tty (`--headless` is the explicit override). **B2** `_wait_input`'s `wait_for(shield(...))` re-raised the reader's exception, making `_line_or_empty` unreachable, and `run_cli` caught only four types so an `OSError` or a `TypeError` exited **1** ("the run failed") with a bare traceback: the presenter now handles reader faults internally and `run_cli` is a real boundary (`testlab_cli_failed` -> exit 7, traceback on stderr). **B3** `JARVIS_TESTLAB_MAX_COST_USD=1e9` passed `_max_cost_usd` and was refused by `ResourceGrant`, so `build_test_lab()` raised for EVERY command and `ControlCenter.__init__` could not construct — the budget is clamped to `MAX_PROFILE_COST_USD`, the grant construction is caught, and the reason travels as `TestLabConfig.grant_refusal` into `status` and onto every CLI invocation. **S1** `_body` used `request.content.read(n)` (buffered, not n bytes), so 200 KB+ bodies read truncated and were refused as malformed JSON — now `request.read()` with a 413 `testlab_http_too_large` that says the size. **S2** a 5xx returned the raw exception message (QA extracted a credential and an absolute path); it now answers a fixed sentence and journals the detail. **S3** an expired step was re-announced every poll (25 headers/expiries for one step) — one `expired_for` key. **S4** `NEEDS_SUPERVISOR` named `maintain`, which is not a command, and missed `retention --apply`, which calls `supervisor.maintain()`; it is now the predicate `needs_supervisor(args)`. **S5** the narrowing grant is documented as a complete three-field document with an example. Nits: the live line says "Ctrl-C stops the run"; a cancelled sweep exits 6, not 1 (`SWEEP_EXIT`); `-p x=Infinity` is refused where it was typed instead of deep inside a value guard; `status` paths show the home as `~`. 29 new tests (presenter 7, composition 16, CLI 11, HTTP 9, integration 7 across both files). 1534 unit + `test_documented_routes`, 43 Slice 10 integration, 82 narrow voice, 250 Control Center, all green; `runtime/testlab` still never created by the suite.
- 2026-09-18 Slice 10 rework 2 (PM: ordering + a guard): the tty refusal ran AFTER `api.start()`, so `echo | ... run --profile hardware:guided --guided` refused but still created `runtime/testlab/work/`.supervisor.lock` — a command that does nothing littered a machine and locked out another process for the moment it took to refuse. New pure `preflight(args)` (`cli.py:772-795`) runs BEFORE a Test Lab is composed (assignments, axes, the guided tty rule), and `run_cli` no longer starts the supervisor: each command calls `_take_work_root()` after its own checks, so `run` reads its scenario file and `describe`s the diagnostic first, `sweep` builds and describes its spec first, `cancel` reads the run first, and `retention --apply` takes it only then. Measured on a clean tree, all five refusal paths now create NOTHING (guided 2, unknown diagnostic 7, bad `-p` 2, unknown run id 7, retention 0). `NEEDS_SUPERVISOR` is now documented as "may take", not "does", and an integration test checks the declaration against behaviour. Guard against the class of bug I hit myself: an autouse fixture in `tests/conftest.py` (only for `test_testlab*` modules) records every `TestLab.__init__` root and fails the test if one resolves inside the repository, and fails again if `runtime/testlab` appeared during it (removing it either way) — proven by a probe test that composes `build_test_lab()` with no root and is refused by name. 16 new tests (13 parametrized refusal/read cases, the guided-refusal case, the declaration-vs-behaviour case, the probe run manually). 1534 unit, 73 testlab integration, 302 Control Center + narrow voice, all green; `runtime/testlab` absent afterwards.
- 2026-09-18 Slice 10 rework 3 (PM S-A, S-B + the HTTP ordering nit + two consistency lines): **S-A** `_read_result` (`presenter.py:288-303`) now maps a plain `""` to `NO_INPUT` as well as cancelled/raised/non-string — unreachable through today's CLI, but the Slice 11 UI, a websocket or SSH reader or any double that ends its stream with `""` would have resurrected B1 one layer out. The reader contract is now "return the line WITH its newline"; the test doubles send `ENTER = "
"`, and a new test drives a reader returning `""` and asserts nothing is acknowledged. **S-B** the countdown was `write=console.note`, one appended stderr line every 0.5 s (about 16 lines for an 8 s step, 60 for a 30 s one), so the header and the phrase the operator must SAY scrolled out of view while `Console.tick` sat unused: new `write_live=` seam on `GuidedPresenter` (`:171-181`, defaults to `write`), both CLI call sites pass `console.tick`, and `Console` now owns its cursor (`cli.py:465-508`) — `note`/`report` close an in-place line first, so the bar moves under a header that stays. **HTTP ordering**, the same defect the CLI had: `POST /runs` took the work root before answering 400/404. New shared `TestLabApi.check_run_request` (`api.py:213-228`) validates the profile, the ad-hoc scenario and the declaration — all catalog questions, no lock — and both surfaces call it before `_take_work_root`/`_supervised`. Swept the other POSTs: `/sweeps` decodes and resolves its spec first, `/runs/{id}/cancel` reads the run first, `/sweeps/{id}/cancel` needs no supervisor at all, `/bundles` and the prompt pair never did. Measured: 7 refused HTTP bodies, all with `testlab/` absent afterwards. Two consistency lines: under `--json` a usage refusal now prints `{ok:false, code:"testlab_cli_usage"}` like every other failure (argparse's own exit 2 stays argparse's and is documented), and the `testlab_cli_failed` payload carries a FIXED sentence with the type, message and traceback on stderr only — the same sanitisation the HTTP 500 boundary got. 18 new tests (presenter 4, CLI 5, HTTP 9). 1552 unit + documented routes, 59 Slice 10 integration, 302 Control Center + narrow voice; `runtime/testlab` absent throughout.
- 2026-09-18 Slice 09 rework 5 (PM: the last flaky test): `test_a_declaration_that_blocks_on_the_positive_claim_reports_the_zero_instead` failed **3 times in 10 runs of the three-file set** while passing alone, and the branch it landed in was NOT the anti-vacuity refusal I expected — the retry helper had already removed that one. The captured evidence says `barge_in_confirmed: 1`: the deliberately-quiet person (`voice_peak=0.03`, about -30 dBFS) sometimes **did** open the gate, so the premise "they spoke and were not heard" was itself a coin flip. Two attempts to make the scenario deterministic, both measured and both rejected: **lowering the voice** to -40 dBFS — 19 dB under this room's in-window echo and still 10 dB clear of `VOICE_FLOOR_DBFS` — was clean 12/12 in isolation but still failed **3 of 10 suite runs**, because `heard_voice_since` counts near-end frames CUMULATIVELY over a window whose length moves with the machine's scheduling (a passing attempt had 17 near frames over 220, a failing one 45 over 270); and **taking the person out of the utterance** — speaking before it starts, or only after it ends — was confirmed **16/16 and 16/16** respectively, because the measurement window outlives the output. The two levels cannot be separated: "certifiably in the room" and "loud enough for the gate" are the same microphone measurement a few dB apart. So the test asserts the SHAPE, the way S1 was fixed, and it is stronger than the single-outcome version it replaces: the person must be certifiably present on EVERY attempt (the zero is a result, never a failure to observe); the unheard branch must OCCUR at least once or the test fails saying the rule was never exercised; every unheard attempt must come back `failed`; and every attempt where the gate DID open must come back `passed` from the same declaration — which is what stops this from being "accept both outcomes", since a runner that failed every guided run, or passed every one, is caught by one half or the other. Measured over 20 independent attempts: **19 unheard/`failed`, 1 heard/`passed`, 0 refusals**, so both halves are live and the chance of all four attempts missing the branch is about 6e-6. The operator script's retry entry is now reachable from the first screen: a callout under the summary points at a real `### If it stops without a result` heading, so the one-session-in-ten honest refusal is answered without reading the page. No product code changed this round. Counts: three-file set **12 consecutive runs, 97 passed / 4 skipped, zero failures** (against 3 failures in 10 before).
- 2026-09-18 PM: Slices 09 and 10 approved and committed together (they were reworked in one tree — see the orchestration note below). Slice 09 took 4 QA rounds: the guided verdict was a coin flip at the declared defaults (3 wrong verdicts in 10 against a compliant human, now 0 in 30), the positive claim was discarded precisely when the barge-in succeeded (0/5 measured, now 29/30), and the finding the diagnostic exists to report was unreportable (now visible at 10/20). Along the way an empty room could certify a human interrupt, and the near-end signal turned out to be an EDGE that latches on the echo and hides a real person. Slice 10 took 3 QA rounds: the guided presenter answered for the human at EOF, an unreadable stdin escaped with a product-failure exit code, and one mistyped env var made the Control Center unbootable. NOT VERIFIED: no real device, no human prompt, no provider call — HV-TL-HW-01 is that gate. ORCHESTRATION NOTE: I ran both implementers in this one checkout concurrently, which made every test tally ambiguous and sent a phantom failure to QA. One implementer per tree from now on; QA in parallel is fine.
