# Reconstructed planning session — Conversation observability

> Reconstructed faithfully from the current discussion; this is not a verbatim transcript.

The user reported that transcripts must become systematic, faithful backend artifacts rather than text regenerated by a model. The record should remain available even when execution fails, be searchable, and become part of Jarvis's debugging culture. The user requested a live full-screen transcript/debug view on a dark/blurred background with a chronological axis: User messages on the left in white, Jarvis/Mouth in light blue, Brain activity in orange, and sub-agent calls in red blocks showing their task name and duration. Brain/Mouth/sub-agent entries should be inspectable and linked to traces. The assistant proposed a durable append-only Conversation Event Log, with readable transcript/export derived from structured events and diagnostics kept as a separate but linked trace layer.

Repository inspection for this handoff found an existing `RuntimeJournal` writing `runtime/trace.jsonl`, a scalar `trace_summary.py`, Brain speech/event contracts, and current voice telemetry. The new task must extend/reconcile these instead of duplicating them.
