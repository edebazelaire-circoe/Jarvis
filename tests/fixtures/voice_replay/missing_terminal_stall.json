{
  "schema": "jarvis.voice_replay",
  "schema_version": 1,
  "scenario_id": "missing_terminal_stall",
  "origin": "2026-09-11T15:47:08+02:00",
  "provenance": {
    "source_path": "tasks/jarvis_voice_architectures_handoff/sources/transcript-2026-09-11.md",
    "source_kind": "human_trace_reconstruction",
    "reported": [
      {"source_ref": "line 332", "fact": "Three speech outputs lacked a terminal event before the stall timeout."}
    ],
    "derived": [
      {"source_ref": "TASK.md lines 32 and 49", "fact": "A missing terminal must recover without inventing completion or heard evidence."}
    ],
    "constructed": [
      {"source_ref": "fixture design", "fact": "Output identifier and 30000 ms probe interval are synthetic because the reconstruction lacks output IDs."}
    ]
  },
  "steps": [
    {"at_ms": 0, "port": "provider", "action": "output_started", "data": {"output_id": "stall-output"}},
    {"at_ms": 30000, "port": "device", "action": "release", "data": {"output_id": "stall-output", "provider_still_active": false}},
    {"at_ms": 30000, "port": "control", "action": "checkpoint", "data": {"checkpoint_id": "stall_recovery"}}
  ]
}
